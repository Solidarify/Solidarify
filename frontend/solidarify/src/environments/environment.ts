export const environment = {
  production: false,
  // IMPORTANTE: Si usas Android/iOS físico, pon tu IP local (ej: 192.168.1.35)
  // Si usas navegador, localhost vale.
  apiUrl: 'http://localhost:3000/api' 
};
